apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: tc-injector
  namespace: tc-injector-system
  labels:
    app: tc-injector
spec:
  selector:
    matchLabels:
      app: tc-injector
  updateStrategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 8
  template:
    metadata:
      labels:
        app: tc-injector
    spec:
      serviceAccountName: tc-injector
      hostNetwork: true
      # hostPID is required so that /proc/<pid>/ns/net of target containers
      # is reachable from within the DaemonSet pod.
      hostPID: true
      tolerations:
        - operator: Exists
          effect: NoSchedule
        - operator: Exists
          effect: NoExecute
      containers:
        - name: tc-injector
          image: quay.io/manabu.ori/tc-injector:v1
          imagePullPolicy: IfNotPresent
          command: ["/tc-injector"]
          args:
            # --cri-socket is optional: omit it to enable auto-detection.
            # Set explicitly if your runtime socket path is non-standard.
            # - --cri-socket=/run/containerd/containerd.sock  # containerd
            # - --cri-socket=/run/crio/crio.sock              # CRI-O
            - --metrics-bind-address=:9988
            - --health-probe-bind-address=:9989
          env:
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
          securityContext:
            privileged: false
            capabilities:
              add:
                - NET_ADMIN    # Required for tc(8)/netem and netlink operations.
                - SYS_ADMIN    # Required for setns(2) to enter pod network namespaces.
                - SYS_PTRACE   # Required to access /proc/<pid>/ns/net for arbitrary PIDs (ptrace_may_access check).
            runAsUser: 0      # Root is required to open /proc/<pid>/ns/net for arbitrary PIDs.
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
          livenessProbe:
            httpGet:
              path: /healthz
              port: 9989
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /readyz
              port: 9989
            initialDelaySeconds: 5
            periodSeconds: 10
          volumeMounts:
            # containerd socket directory (present on containerd nodes).
            - name: containerd-socket-dir
              mountPath: /run/containerd
            # CRI-O socket directory (present on CRI-O nodes).
            - name: crio-socket-dir
              mountPath: /run/crio
            # Host /proc lets us open /proc/<pid>/ns/net for any container.
            - name: host-proc
              mountPath: /proc
              readOnly: true
            # CRI-O stores named netns files here; mount with propagation so
            # newly created namespaces become visible inside the pod.
            - name: host-netns
              mountPath: /var/run/netns
              mountPropagation: HostToContainer
      volumes:
        - name: containerd-socket-dir
          hostPath:
            path: /run/containerd
            type: DirectoryOrCreate   # Created as empty dir on CRI-O nodes where containerd is absent.
        - name: crio-socket-dir
          hostPath:
            path: /run/crio
            type: DirectoryOrCreate   # Created as empty dir on containerd nodes where CRI-O is absent.
        - name: host-proc
          hostPath:
            path: /proc
        - name: host-netns
          hostPath:
            path: /var/run/netns
            type: DirectoryOrCreate
